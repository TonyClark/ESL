
act a {
  fork() = {
    new a <- Count(1)
  }
  Time(t) -> fork();
  Count(0) -> print(0);
  Count(n) -> {
    print(n);
    self <- Count(n-1)
  }
}

Time(n ?(n=0)) -> 
  probably(50) {
    print('hooray')
  } else {
    print('boo')
  };
Time(t) -> {
  new a
}
f(x) = x + 10;

g(a,b) = a + b;

y = g(f(g(f(100),f(100))),f(100)); 

addTree(t) = 
  case t {
    Tree(left,right) ->
      (addTree(left)) + addTree(right);
    Leaf(n) -> n
  };
  
map(f,l) =
  case l {
    [a,b,c] -> [3];
    h:t -> (f(h)):map(f,t);
    [] -> []
  };

//main() = addTree(Tree(Leaf(y),Tree(Leaf(200),Leaf(300))))

main() = map(fun(x) [f(x)],[1,2,3,4,5,6,7,8,9])
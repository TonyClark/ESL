
act a {
  fork() = {
    let x = new a in x <- Count(10,x)
  }
  Time(t) -> fork();
  Count(0,_) -> print(0);
  Count(n,self) -> {
    print(n);
    self <- Count(n-1,self)
  }
}

Time(0) -> print('hooray');
Time(t) -> {
  new a
}